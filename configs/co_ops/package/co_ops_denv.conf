# denv-specific configuration for generate_co_ops_installation_package.sh

EXPECTED_CURRENT_HOST="gbl25149108.hc.cloud.uk.hsbc"
TARGET_HOST_SUFFIX=".hk.hsbc"
BUILD_BASE_DIR="/FCR_APP/abinitio/tmp"
BUILD_SCRIPT_RELATIVE_PATH="ai_build/scripts.v2/build-package.ksh"
BUILD_REFERENCE_XREF=""
REMOTE_USER="fap41-abiadmin"
REMOTE_PACKAGE_ROOT="/opt/abinitio/tmp"
UPGRADE_SCRIPT_SOURCE="/FCR_APP/abinitio/tmp/upgrade_co_ops_automation.sh"
UPGRADE_CONFIG_SOURCE="${REPO_ROOT}/configs/co_ops/upgrade/co_ops_upgrade_denv.conf"
SSH_AUTH_MODE="password"
PACKAGE_ENV_LABEL="denv"
PACKAGE_MIN_SIZE_MB=500

# Replace these sample hosts with the real approved denv targets.
TARGET_HOST_ALLOWLIST=(
  "gbl25149199.hk.hsbc"
  "gbl25149200.hk.hsbc"
)

# Shared runtime settings. Override them here later if you need different
# locations without changing the script.
LOG_DIR="${REPO_ROOT}/logs"
STATE_DIR="${REPO_ROOT}/.state"
LOG_RETENTION_DAYS=90
